# cron-job.org API token
# Wygeneruj na: https://console.cron-job.org/settings → API → Create API key
CRON_JOB_ORG_API_KEY=

# Opcjonalne: default timezone dla nowych jobs (jeśli user nie podaje)
# CRON_JOB_ORG_DEFAULT_TIMEZONE=Europe/Warsaw
